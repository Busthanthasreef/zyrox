<div class="mobile-bottom-nav d-lg-none">
    <a href="/myOrders" class="nav-item <%= activePage === 'orders' ? 'active' : '' %>">
        <i class="fas fa-clipboard-list"></i>
        <span>Orders</span>
    </a>
    <a href="/wallet" class="nav-item <%= activePage === 'wallet' ? 'active' : '' %>">
        <i class="fas fa-wallet"></i>
        <span>Wallet</span>
    </a>
    <a href="/address" class="nav-item <%= activePage === 'address' ? 'active' : '' %>">
        <i class="fas fa-location-dot"></i>
        <span>Address</span>
    </a>
    <a href="/profile" class="nav-item <%= activePage === 'profile' ? 'active' : '' %>">
        <div class="nav-avatar">
            <img src="<%= (typeof user !== 'undefined' && user && user.Profile_image) ? user.Profile_image : (typeof userProfileImage !== 'undefined' ? userProfileImage : '/images/default-avatar.png') %>" alt="P">
        </div>
        <span>Profile</span>
    </a>
</div>

<style>
    .mobile-bottom-nav {
        position: fixed;
        bottom: 0;
        left: 0;
        right: 0;
        background: #141416;
        border-top: 1px solid rgba(255, 255, 255, 0.05);
        display: flex;
        justify-content: space-around;
        padding: 10px 0 25px; /* Extra padding for iOS home bar */
        z-index: 1050;
        backdrop-filter: blur(15px);
        box-shadow: 0 -10px 30px rgba(0,0,0,0.5);
    }

    .mobile-bottom-nav .nav-item {
        display: flex;
        flex-direction: column;
        align-items: center;
        text-decoration: none;
        color: #7b8fb5;
        font-size: 0.6rem;
        font-weight: 500;
        gap: 5px;
        transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        width: 20%;
    }

    .mobile-bottom-nav .nav-item i {
        font-size: 1rem;
        transition: transform 0.3s ease;
    }

    .mobile-bottom-nav .nav-item.active {
        color: #4f6ef7;
    }

    .mobile-bottom-nav .nav-item.active i {
        transform: translateY(-2px);
    }

    .nav-avatar {
        width: 22px;
        height: 22px;
        border-radius: 50%;
        overflow: hidden;
        border: 1.5px solid #7b8fb5;
        transition: all 0.3s ease;
    }

    .nav-avatar img {
        width: 100%;
        height: 100%;
        object-fit: cover;
    }

    .nav-item.active .nav-avatar {
        border-color: #4f6ef7;
        transform: translateY(-2px);
    }
</style>
