<header class="mobile-top-header d-lg-none">
    <div class="header-logo">Zyrox<span class="dot">.</span></div>
</header>

<style>
    .mobile-top-header {
        display: flex;
        align-items: center;
        padding: 16px 20px;
        background: var(--bg-deep, #0c0f1a);
        position: sticky;
        top: 0;
        z-index: 1000;
        border-bottom: 1px solid rgba(255, 255, 255, 0.05);
    }
    .header-logo { font-size: 1.5rem; font-weight: 800; letter-spacing: -0.5px; color: #fff; }
    .header-logo .dot { color: var(--primary-blue, #4f6ef7); }
</style>
